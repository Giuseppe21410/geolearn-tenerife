import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.geolearntenerife.app',
  appName: 'GeoLearnTenerife',
  webDir: 'dist'
};

export default config;
